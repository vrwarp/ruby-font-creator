[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "zi2zi-jit"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["data_processing*", "util*"]
